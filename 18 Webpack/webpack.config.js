const webpack = require("webpack")

module.exports = {
    mode: "development", // Seta o mode como desenvolvimento. O modo "production" faz uma minificação do arquivo
    entry: "./src/principal.js" // Ponto de entrada do Webpack
}